---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: kyc-service
  namespace: game_engine
  labels:
    app: kyc-service
    version: v1
    component: service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: kyc-service
  template:
    metadata:
      labels:
        app: kyc-service
        version: v1
        component: service
    spec:
      terminationGracePeriodSeconds: 30
      containers:
        - name: kyc-service
          image: registry.game_engine.local:5000/kyc-service:latest
          imagePullPolicy: Always
          ports:
            - name: grpc
              containerPort: 9131
              protocol: TCP
          env:
            - name: GRPC_PORT
              value: "9131"
          resources:
            requests:
              cpu: 125m
              memory: 64Mi
            limits:
              cpu: 250m
              memory: 128Mi
          livenessProbe:
            grpc:
              port: 9131
            initialDelaySeconds: 10
            periodSeconds: 15
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            grpc:
              port: 9131
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
---
apiVersion: v1
kind: Service
metadata:
  name: kyc-service
  namespace: game_engine
  labels:
    app: kyc-service
spec:
  type: ClusterIP
  ports:
    - name: grpc
      port: 9131
      targetPort: 9131
      protocol: TCP
  selector:
    app: kyc-service
