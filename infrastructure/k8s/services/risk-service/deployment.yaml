---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: risk-service
  namespace: game_engine
  labels:
    app: risk-service
    version: v1
    component: service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: risk-service
  template:
    metadata:
      labels:
        app: risk-service
        version: v1
        component: service
    spec:
      terminationGracePeriodSeconds: 30
      containers:
        - name: risk-service
          image: registry.game_engine.local:5000/risk-service:latest
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 9016
              protocol: TCP
            - name: grpc
              containerPort: 9116
              protocol: TCP
          env:
            - name: PORT
              value: "9016"
            - name: GRPC_PORT
              value: "9116"
          resources:
            requests:
              cpu: 125m
              memory: 64Mi
            limits:
              cpu: 250m
              memory: 128Mi
          livenessProbe:
            grpc:
              port: 9116
            initialDelaySeconds: 10
            periodSeconds: 15
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            grpc:
              port: 9116
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
---
apiVersion: v1
kind: Service
metadata:
  name: risk-service
  namespace: game_engine
  labels:
    app: risk-service
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: 9016
      protocol: TCP
    - name: grpc
      port: 9116
      targetPort: 9116
      protocol: TCP
  selector:
    app: risk-service
