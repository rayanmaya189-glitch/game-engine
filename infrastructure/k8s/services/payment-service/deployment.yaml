---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: payment-service
  namespace: game_engine
  labels:
    app: payment-service
    version: v1
    component: service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: payment-service
  template:
    metadata:
      labels:
        app: payment-service
        version: v1
        component: service
    spec:
      terminationGracePeriodSeconds: 30
      containers:
        - name: payment-service
          image: registry.game_engine.local:5000/payment-service:latest
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 9012
              protocol: TCP
            - name: grpc
              containerPort: 9112
              protocol: TCP
          env:
            - name: PORT
              value: "9012"
            - name: GRPC_PORT
              value: "9112"
          resources:
            requests:
              cpu: 125m
              memory: 64Mi
            limits:
              cpu: 250m
              memory: 128Mi
          livenessProbe:
            grpc:
              port: 9112
            initialDelaySeconds: 10
            periodSeconds: 15
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            grpc:
              port: 9112
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
---
apiVersion: v1
kind: Service
metadata:
  name: payment-service
  namespace: game_engine
  labels:
    app: payment-service
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: 9012
      protocol: TCP
    - name: grpc
      port: 9112
      targetPort: 9112
      protocol: TCP
  selector:
    app: payment-service
