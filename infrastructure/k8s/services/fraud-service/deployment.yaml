---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: fraud-service
  namespace: game_engine
  labels:
    app: fraud-service
    version: v1
    component: service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: fraud-service
  template:
    metadata:
      labels:
        app: fraud-service
        version: v1
        component: service
    spec:
      terminationGracePeriodSeconds: 30
      containers:
        - name: fraud-service
          image: registry.game_engine.local:5000/fraud-service:latest
          imagePullPolicy: Always
          ports:
            - name: grpc
              containerPort: 9115
              protocol: TCP
          env:
            - name: GRPC_PORT
              value: "9115"
          resources:
            requests:
              cpu: 125m
              memory: 64Mi
            limits:
              cpu: 250m
              memory: 128Mi
          livenessProbe:
            grpc:
              port: 9115
            initialDelaySeconds: 10
            periodSeconds: 15
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            grpc:
              port: 9115
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
---
apiVersion: v1
kind: Service
metadata:
  name: fraud-service
  namespace: game_engine
  labels:
    app: fraud-service
spec:
  type: ClusterIP
  ports:
    - name: grpc
      port: 9115
      targetPort: 9115
      protocol: TCP
  selector:
    app: fraud-service
