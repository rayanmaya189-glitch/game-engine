apiVersion: apps/v1
kind: Deployment
metadata:
  name: aml-service
  namespace: game-engine
  labels:
    app: aml-service
    tier: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: aml-service
  template:
    metadata:
      labels:
        app: aml-service
        tier: backend
    spec:
      containers:
        - name: aml-service
          image: game-engine/aml-service:latest
          ports:
            - containerPort: 9114
              name: grpc
          env:
            - name: DB_HOST
              valueFrom:
                configMapKeyRef:
                  name: game-engine-config
                  key: postgres-host
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: game-engine-secrets
                  key: postgres-password
          resources:
            requests:
              memory: "64Mi"
              cpu: "100m"
            limits:
              memory: "128Mi"
              cpu: "250m"
          livenessProbe:
            exec:
              command: ["/bin/grpc_health_probe", "-addr=:9114"]
            initialDelaySeconds: 10
            periodSeconds: 10
          readinessProbe:
            exec:
              command: ["/bin/grpc_health_probe", "-addr=:9114"]
            initialDelaySeconds: 5
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: aml-service
  namespace: game-engine
spec:
  selector:
    app: aml-service
  ports:
    - port: 9114
      targetPort: 9114
      name: grpc
  type: ClusterIP
