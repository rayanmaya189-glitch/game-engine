apiVersion: apps/v1
kind: Deployment
metadata:
  name: fraud-service
  namespace: game-engine
  labels:
    app: fraud-service
    tier: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: fraud-service
  template:
    metadata:
      labels:
        app: fraud-service
        tier: backend
    spec:
      containers:
        - name: fraud-service
          image: game-engine/fraud-service:latest
          ports:
            - containerPort: 9115
              name: grpc
          env:
            - name: DB_HOST
              valueFrom:
                configMapKeyRef:
                  name: game-engine-config
                  key: postgres-host
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: game-engine-secrets
                  key: postgres-password
          resources:
            requests:
              memory: "64Mi"
              cpu: "100m"
            limits:
              memory: "128Mi"
              cpu: "250m"
          livenessProbe:
            exec:
              command: ["/bin/grpc_health_probe", "-addr=:9115"]
            initialDelaySeconds: 10
            periodSeconds: 10
          readinessProbe:
            exec:
              command: ["/bin/grpc_health_probe", "-addr=:9115"]
            initialDelaySeconds: 5
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: fraud-service
  namespace: game-engine
spec:
  selector:
    app: fraud-service
  ports:
    - port: 9115
      targetPort: 9115
      name: grpc
  type: ClusterIP
