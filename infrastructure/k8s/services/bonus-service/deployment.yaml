---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: bonus-service
  namespace: game_engine
  labels:
    app: bonus-service
    version: v1
    component: service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: bonus-service
  template:
    metadata:
      labels:
        app: bonus-service
        version: v1
        component: service
    spec:
      terminationGracePeriodSeconds: 30
      containers:
        - name: bonus-service
          image: registry.game_engine.local:5000/bonus-service:latest
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 9013
              protocol: TCP
            - name: grpc
              containerPort: 9113
              protocol: TCP
          env:
            - name: PORT
              value: "9013"
            - name: GRPC_PORT
              value: "9113"
          resources:
            requests:
              cpu: 125m
              memory: 64Mi
            limits:
              cpu: 250m
              memory: 128Mi
          livenessProbe:
            grpc:
              port: 9113
            initialDelaySeconds: 10
            periodSeconds: 15
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            grpc:
              port: 9113
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
---
apiVersion: v1
kind: Service
metadata:
  name: bonus-service
  namespace: game_engine
  labels:
    app: bonus-service
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: 9013
      protocol: TCP
    - name: grpc
      port: 9113
      targetPort: 9113
      protocol: TCP
  selector:
    app: bonus-service
